# Configuration Nginx — frontend statique 4mation.lab211.fr
# Fichier suggéré : /etc/nginx/sites-available/4mation-frontend

server {
    listen 80;
    server_name 4mation.lab211.fr;

    root /var/www/4mation;
    index index.html;

    access_log /var/log/nginx/4mation_frontend_access.log;
    error_log /var/log/nginx/4mation_frontend_error.log;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Configuration Nginx — API api-4mation.lab211.fr
# Fichier suggéré : /etc/nginx/sites-available/4mation-api
#
# IMPORTANT : le multijoueur Socket.IO tourne sur un processus dédié (port 8098).
# L'API REST Gunicorn est sur le port 8097 (ou 5000 en install manuelle legacy).

server {
    listen 80;
    server_name api-4mation.lab211.fr;

    client_max_body_size 10M;

    access_log /var/log/nginx/4mation_api_access.log;
    error_log /var/log/nginx/4mation_api_error.log;

    location /socket.io/ {
        proxy_pass http://127.0.0.1:8098;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 86400s;
        proxy_send_timeout 86400s;
    }

    location / {
        proxy_pass http://127.0.0.1:8097;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 120s;
        proxy_send_timeout 120s;
        proxy_read_timeout 120s;
    }
}

# HTTPS : activer après certificat Let's Encrypt (certbot)
# Répéter les blocs ci-dessus sur listen 443 ssl http2 avec :
#   ssl_certificate /etc/letsencrypt/live/4mation.lab211.fr/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/4mation.lab211.fr/privkey.pem;
